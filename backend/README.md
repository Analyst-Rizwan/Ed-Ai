# EduAi Backend v1.1

FastAPI backend for EduAi. Includes JWT auth, OpenAI integration, mock progress + roadmap APIs.

## 🧩 Setup

```bash
cd backend
python -m venv .venv
.venv\Scripts\activate  # (Windows)
pip install -r requirements.txt
